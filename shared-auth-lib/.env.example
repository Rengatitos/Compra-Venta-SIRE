# Variables esperadas por los modulos que usan shared-auth-lib.
# Esta libreria no se ejecuta como servicio, pero estos valores deben
# existir en los backends consumidores.

JWT_SECRET_KEY=change-this-secret
JWT_ALGORITHM=HS256
JWT_ISSUER=auth-api
JWT_AUDIENCE=internal-apis
